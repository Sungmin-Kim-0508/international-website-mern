export type programLabels = {
  title: string;
  content: string;
};

export type note = {
  title: string;
  date: string;
  body: string;
  src: string;
};

export type rowDirectionCard = {
  src?: string;
  alt?: string;
  to: string;
};
