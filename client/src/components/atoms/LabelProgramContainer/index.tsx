import React from "react";
import * as Sc from "./style";

const LabelProgramContainer: React.FC = ({ children }) => (
  <Sc.LabelProgram>{children}</Sc.LabelProgram>
);

export default LabelProgramContainer;
